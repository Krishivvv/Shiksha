# ── Secrets & environment ──
.env
.env.local
.env.*.local
**/.env
**/.env.local
!**/.env.example

# ── Tooling / local config ──
.claude/

# ── Python ──
__pycache__/
*.pyc
*.pyo

# ── Databases (never commit application data) ──
*.db
*.sqlite
*.sqlite3
instance/

# ── Generated / runtime artifacts ──
output/
output.mp4
uploads/
segments/
voice/
final_videos/
pdf_images/
migrations/
temp_render.html

# ── Dependencies ──
/node_modules
**/node_modules

# ── Build output (committed build is regenerated; ignore stray root build) ──
/build
